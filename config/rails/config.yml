colang_version: "2.x"

models:
  - type: main
    engine: openai
    model: gpt-4o-mini

rails:
  input:
    flows: []
  
  dialog:
    flows:
      - main
  
  output:
    flows: []

config:
  flow_logging:
    enabled: true
